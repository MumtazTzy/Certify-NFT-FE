services:
  certify-nft-fe:
    container_name: certify-nft-fe
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
    restart: unless-stopped
    networks:
      - certify-nft-network

networks:
  certify-nft-network:
    external: true